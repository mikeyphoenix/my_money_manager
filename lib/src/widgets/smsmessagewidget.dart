import 'package:flutter/material.dart';

class SmsMessageWidget extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Card (
      child: Text("Some Text goes here"),
    );
  }
}